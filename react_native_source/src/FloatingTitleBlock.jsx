import React from 'react';
import {
  View,
  Image,
  StyleSheet,
  Button,
  TouchableOpacity
} from 'react-native';

const FloatingTitleBlock = (props) => {
  return (
    <View >
      {/*
      - haml_tag "h#{@level + 1}##{@id}", title, :class => [@style, role, ('slide' if option? :step)]


      */}

    </View>
  );
};


const styles = StyleSheet.create({
  contactPanel: {fontWeight: "700"},
});

export default FloatingTitleBlock;
